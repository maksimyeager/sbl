import Breadcrumb from "../components/Breadcrumb";

const OurProjects = () => {
    return (
        <div className="container">
            <Breadcrumb />
            <h1 className="title-1">Наши проекты</h1>
        </div>
    );
};

export default OurProjects;
